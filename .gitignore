# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env files — keep .env.example committed (build-time prefills only).
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test artifacts
coverage

# TypeScript incremental build info (from `tsc -b`)
*.tsbuildinfo

# Generated types are committed for reproducible builds, but you can delete
# this line if you'd rather regenerate locally and keep them out of git.
# src/api/schema.ts